import 'package:flutter/material.dart';

class CommunityPostsReportScreen extends StatelessWidget {
  const CommunityPostsReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Community Posts Report')),
      body: const Center(child: Text('Community Posts Report content coming soon')),
    );
  }
}
