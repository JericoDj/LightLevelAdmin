import 'package:flutter/material.dart';

class BookingsFaceToFaceReportScreen extends StatelessWidget {
  const BookingsFaceToFaceReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bookings Face to Face Report')),
      body: const Center(child: Text('Face to Face Report content coming soon')),
    );
  }
}
