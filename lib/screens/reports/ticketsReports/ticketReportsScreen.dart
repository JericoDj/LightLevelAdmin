import 'package:flutter/material.dart';

class TicketsReportScreen extends StatelessWidget {
  const TicketsReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tickets Report')),
      body: const Center(child: Text('Tickets Report content coming soon')),
    );
  }
}
