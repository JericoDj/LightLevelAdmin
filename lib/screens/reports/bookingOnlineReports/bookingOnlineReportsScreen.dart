import 'package:flutter/material.dart';

class BookingsOnlineReportScreen extends StatelessWidget {
  const BookingsOnlineReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bookings Online Report')),
      body: const Center(child: Text('Bookings Online Report content coming soon')),
    );
  }
}
