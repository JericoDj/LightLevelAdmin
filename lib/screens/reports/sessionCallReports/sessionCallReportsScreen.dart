import 'package:flutter/material.dart';

class SessionCallsReportScreen extends StatelessWidget {
  const SessionCallsReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Session Calls Report')),
      body: const Center(child: Text('Session Calls Report content coming soon')),
    );
  }
}
